import * as React from 'react';

export interface AddPostPageProps {
}

export default function AddPostPage(props: AddPostPageProps) {
    return (
        <div>
            Thêm mới post
        </div>
    );
}
